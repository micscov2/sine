#!/usr/bin/python

# A program to work with regular expressions
# No warranty implied or whatsoever

# A c-module for working with regular expressions
import re

# Global variables
regex_str = 'MOD [A-Z]+:\s;'
pattern = re.compile(regex_str)

def main():
	pass

if __name__ == '__main__':
	main()

